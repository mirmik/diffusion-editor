# Published/editable package metadata.
#
# Termin distributions stay unpinned here because their exact +sdk build IDs
# come from python-runtime-manifest.json. install-deps.sh does not resolve this
# file with PyPI; it installs the SDK closure separately.
tcbase
termin-dispatch
termin-display
termin-glb-native
termin-gui-native
termin-mcp
tgfx
numpy==2.5.1
Pillow==12.3.0
PySDL2==0.9.17
PyYAML==6.0.3
watchdog==6.0.0
